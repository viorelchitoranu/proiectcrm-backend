<?xml version="1.0" encoding="UTF-8"?>

<!--
    Configurare Logback pentru ProiectCRM.

    Profiluri:
      "dev"  → console cu culori, fara fisiere
      "prod" → fisiere cu rotatie zilnica, fara console

    Fisiere produse in productie (la calea ${logging.file.path}):
      app.log    → toate evenimentele INFO+ (30 zile, max 1 GB)
      error.log  → WARN si ERROR (90 zile, max 500 MB)

    FIX: folosim <springProperty> pentru a citi logging.file.path
    din Spring Environment — logback se initializeaza inainte ca
    Spring Boot sa proceseze variabilele de mediu, deci ${logging.file.path}
    era undefined la momentul initializarii (LOG_PATH_IS_UNDEFINED).
    Ref: https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.logging.logback-extensions
-->
<configuration>

    <!-- Citeste proprietatea din Spring Environment dupa ce Spring Boot o proceseaza -->
    <springProperty scope="context" name="LOG_PATH"
                    source="logging.file.path"
                    defaultValue="/app/logs"/>

    <!-- Pattern partajat intre console (dev) si fisier (prod) -->
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [req:%X{requestId:-?}] [user:%X{userEmail:-anon}] [%X{httpMethod:- }%X{httpPath:-}] %logger{36} - %msg%n"/>

    <!-- ===========================================================
         PROFIL DEV: console colorata
         =========================================================== -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) [req:%X{requestId:-?}] [%X{userEmail:-anon}] %logger{30} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

        <logger name="org.springframework"          level="WARN"/>
        <logger name="org.hibernate"                level="WARN"/>
        <logger name="com.zaxxer.hikari"            level="WARN"/>
    </springProfile>

    <!-- ===========================================================
         PROFIL PROD: fisiere cu rotatie, fara console
         =========================================================== -->
    <springProfile name="prod">

        <!-- app.log: toate evenimentele INFO+ -->
        <appender name="FILE_ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/app.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/app.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
                <maxHistory>30</maxHistory>
                <totalSizeCap>1GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>${LOG_PATTERN}</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <!-- error.log: DOAR WARN si ERROR -->
        <appender name="FILE_ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/error.log</file>
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>WARN</level>
            </filter>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/error.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
                <maxHistory>90</maxHistory>
                <totalSizeCap>500MB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>${LOG_PATTERN}</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="FILE_ALL"/>
            <appender-ref ref="FILE_ERROR"/>
        </root>

        <logger name="org.springframework"          level="WARN"/>
        <logger name="org.hibernate"                level="WARN"/>
        <logger name="com.zaxxer.hikari"            level="WARN"/>
        <logger name="org.springframework.security" level="WARN"/>
    </springProfile>

</configuration>